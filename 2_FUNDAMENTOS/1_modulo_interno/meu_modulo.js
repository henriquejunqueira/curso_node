// ? module.exports exporta tudo que tiver dentro dele e poderá ser reutilizado onde for importado
module.exports = {
  // ? quando é exportada uma função no módulo, não é necessário colocar a palavra function
  soma(a, b) {
    console.log(a + b);
  },
};
